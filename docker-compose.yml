services:
  radarbot:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - DATA_DIR=/app/data
    volumes:
      - radarbot_data:/app/data

volumes:
  radarbot_data:
